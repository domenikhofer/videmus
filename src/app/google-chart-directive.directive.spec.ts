import { GoogleChartDirectiveDirective } from './google-chart-directive.directive';

describe('GoogleChartDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleChartDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
